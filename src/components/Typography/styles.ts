import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles({
  h1: {
    margin: "0",
  },

  h2: {
    margin: "0",
  },

  h3: { margin: "0" },

  h4: { margin: "0" },

  h5: { margin: "0" },

  h6: { margin: "0" },

  p: { margin: "0" },
});

export { useStyles };
